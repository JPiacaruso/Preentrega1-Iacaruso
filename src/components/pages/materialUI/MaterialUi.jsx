import { Button } from "@mui/material";
const MaterialUi = () => {
  return (
    <div>
      <Button variant="contained" color="secondary">
        Contained
      </Button>
    </div>
  );
};

export default MaterialUi;
